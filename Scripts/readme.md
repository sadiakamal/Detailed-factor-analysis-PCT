### the command below will create the environment needed run the LLM finetuning scripts.
conda env create -f LLM-config.yml
### to run the scripts please do the following.
python train/finetuning-LLM-classification.py
